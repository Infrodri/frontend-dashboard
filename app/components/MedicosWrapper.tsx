import { fetchFilteredMedicos } from '@/app/helpers/apimedicos';
import React from 'react';

const MedicosWrapper = async () => {
    return (
        <div>
            
        </div>
    );
};

export default MedicosWrapper;